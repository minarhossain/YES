import React from 'react';
import CreateForm from '../Components/CreateForm';

const CreatePage = () => {
    return (
        <div>

            <CreateForm />

        </div>
    );
};

export default CreatePage;